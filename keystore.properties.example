# Copy this file to keystore.properties in the repo root (gitignored).
# Leave passwords empty in any committed copy. Fill them only on a private machine
# or via CI secrets. Paths are relative to the app module or the repo root.
storeFile=keystore/mesh-update.jks
storePassword=
keyAlias=mesh
keyPassword=
